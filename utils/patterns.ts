export const REGEX_NAME_VALIDATION = /^[a-zA-Z]+(?:\s[a-zA-Z]+){1,}$/;

export const REGEX_EMAIL_VALIDATION =
  /^[\w+-.]{1,64}@([\w-]{1,45}\.)+[\w-]{2,4}$/;

export const REGEX_MOBILE_VALIDATION = /^[0-9+ ]+$/;
